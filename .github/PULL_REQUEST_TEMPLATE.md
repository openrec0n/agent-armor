## Description

Brief description of changes.

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run typecheck:lib` passes (core has no DOM dependencies)
- [ ] New threats include `settingsFragment`, `validTargets`, and at least one reference
